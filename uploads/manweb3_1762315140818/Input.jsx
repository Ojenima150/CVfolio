import React from "react";

function Input(props) {
  return <input type={props.type} placeholder={props.placeholder} />;
  //then after that we move to the input elemnet and remove one of the input
}

export default Input;
